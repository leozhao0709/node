import { SignupExceptionFilter } from './signup-exception.filter';

describe('SignupExceptionFilter', () => {
  it('should be defined', () => {
    expect(new SignupExceptionFilter()).toBeDefined();
  });
});
